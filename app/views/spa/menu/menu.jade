a.list-group-item(ng-repeat-start="item in items track by item.value" ng-click="collapsed = !collapsed; filterQuery(item)") {{item.value}} 
    span.badge {{item.count}}
    .i.glyphicon.glyphicon-remove(ng-show="isActive(item)" ng-click="removeFilter(item); $event.preventDefault(); $event.stopPropagation();")
.list-group(ng-repeat-end ng-if="item.pivot" uib-collapse="collapsed")
    ng-include(src="'spa/menu/menuItem.html'")
